<!doctype html>
<html lang="ko">
<head>
  <link rel="icon" type="image/png" href="/public/favicon.png" />
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>관리자 계정 변경 - <%= siteName %></title>
  <link rel="stylesheet" href="/public/styles.css" />
</head>
<body>
  <header class="topbar">
    <div class="wrap topbar-inner topbar-3col">
      <div class="top-left">
        <a class="brand" href="/">
          <img src="/public/logo.png" alt="SNF CORPORATION" />
        </a>
      </div>

      <nav class="top-center">
        <a class="navlink" href="/offers">OFFERS</a>
      </nav>

      <div class="top-right">
        <a class="link" href="/">HOME</a>
      </div>
    </div>
  </header>

  <main class="wrap auth">
    <h1>관리자 계정 변경</h1>

    <% if (sent) { %>
      <div class="alert">✅ 인증코드를 메일로 보냈습니다.</div>
    <% } %>

    <% if (ok) { %>
      <div class="alert">✅ 변경 완료</div>
    <% } %>

    <% if (err) { %>
      <div class="alert">⚠️ 오류: <%= err %></div>
    <% } %>

    <p>현재 관리자: <b><%= admin?.username %></b></p>
    <p>인증코드 수신 메일: <b><%= process.env.ADMIN_EMAIL %></b></p>

    <form method="post" action="/admin/account/send-code" class="form" style="margin-top:16px;">
      <button class="btn" type="submit">인증코드 보내기</button>
    </form>

    <hr style="margin:24px 0;" />

    <form method="post" action="/admin/account/update" class="form">
      <label>새 아이디</label>
      <input name="newUsername" placeholder="원하면 변경" />

      <label>새 비밀번호 (6자 이상)</label>
      <input name="newPassword" type="password" placeholder="원하면 변경" />

      <label>메일 인증코드(6자리)</label>
      <input name="otpCode" inputmode="numeric" placeholder="123456" required />

      <button class="btn" type="submit">변경 확정</button>
    </form>

    <p style="margin-top:16px;">
      <a class="link" href="/admin">관리자 화면으로</a>
    </p>
  </main>
</body>
</html>